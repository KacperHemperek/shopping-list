import { View, Text } from "react-native";
import React from "react";

const ListCard = () => {
    return (
        <View>
            <Text>List</Text>
        </View>
    );
};

export default ListCard;
